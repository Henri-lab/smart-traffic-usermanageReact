# Husky 是一个用于在 Git 钩子上运行脚本的工具。它可以帮助开发团队在提交、推送等 Git 操作前执行预定义的命令，以确保代码质量和一致性。通常情况下，Husky 最常用于以下几个方面：

# 代码规范检查：在提交代码前运行 linter（如 ESLint、Prettier）检查代码风格和质量，以确保符合团队定义的代码规范。

# 单元测试：在推送代码前运行单元测试，确保新代码不会破坏现有功能。

# 提交信息检查：验证提交信息是否符合指定的格式或规范。

# 预提交钩子：在提交前运行自定义的脚本，如格式化代码、生成文档等。